import { defineConfig } from 'astro/config';

export default defineConfig({
  // ↓ここを自分のGitHubユーザー名に書き換えてください
  site: 'https://ryuurei24-ship-it.github.io',
  // ↓ここをリポジトリ名に書き換えてください（先頭に / を付ける）
  base: '/sikou-kiroku',
});
